<!-- res/drawable/dialog_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Establece el color de fondo que especificaste (#FFF1C2) -->
    <solid android:color="#FFF1C2"/>

    <!-- Esquinas redondeadas -->
    <corners android:radius="16dp"/>

    <!-- Margen interno para los elementos del diálogo -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp"/>
</shape>
